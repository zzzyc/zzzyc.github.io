<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>简单背包问题总结</title>
    <url>/2020/05/20/%E7%AE%80%E5%8D%95%E8%83%8C%E5%8C%85%E9%97%AE%E9%A2%98%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p>$1. \ 01$背包：$n$种物品每种一件，求体积为$V$的背包可获得的最大价值<br>$f[i][j]$表示考虑前$i$种物品，体积为$j$的状态<br>状态转移：要么选第$i$种物品，要么不选第$i$种物品<br>$f[i][j] = max(f[i-1][j],f[i-1][j-v_i]+w_i)$<br>滚动数组优化，为了保证状态由前$i-1$个物品转移而来，故逆序枚举体积<br>时间复杂度：$O(nV)$</p>
<hr>
<p>$2.$ 完全背包：$n$种物品每种无限，求体积为$V$的背包可获得的最大价值<br>$f[i][j]$表示考虑前$i$种物品，体积为$j$的状态<br>状态转移：考虑到当前第$i$种物品，这次可以继续选，或者不选<br>$f[i][j]=max(f[i-1][j-k\times v_i]+k\times w_i)(k\geq0)$<br>当$k=0$时自然是$f[i-1][j]$<br>当$k&gt;0$时，为$f[i-1][j-k\times v_i]+k \times w_i$<br>换种思路，考虑$f[i][j]=max(f[i][j],f[i][j-v_i]+w_i)$。即考虑前$i$个物品，体积为$j$，<br>可以在本次选择中不选第$i$种物品$(f[i][j])$，也可选择第$i$种物品$f[i][j-v_i]+w_i$。<br>这是因为每种物品都是无限的。<br>滚动数组优化，因为状态可由$i$转移而来，故正序枚举体积<br>时间复杂度：$O(nV)$</p>
<hr>
<p>$3.$ 多重背包：$n$种背包每种$s_i$个，求体积为$V$的背包可获得的最大价值<br><strong>做法一：</strong><br>$f[i][j]$表示考虑前$i$种物品，体积为$j$的状态，枚举体积。<br>状态转移：第$i$种物品要么不选，要么选$k(k\leq\lfloor \frac{V}{s_i} \rfloor)$个<br>$f[i][j]=max(f[i-1][j],f[i-1][j-k*v[i]]+w[i])$<br>时间复杂度：$O(V(\sum s_i))$</p>
<p><strong>做法二：</strong><br>二进制优化<br>将每种物品按照二进制分为$2^0,2^1,…,2^{k-1},s_i-(2^k-1)$<br>然后做$01$背包即可。<br>时间复杂度：$O(V\sum log(s_i))$</p>
<p><strong>做法三：</strong><br>单调队列优化<br><img src="https://img-blog.csdnimg.cn/20200513153456863.png" alt="ok"><br>由于每种物品只有$s$个，可以取$1,2,3,…,s$个。那么就可以看出是只与前面$s$个状态有关的，<br>取前$s$个状态的最大值，就是维护一个价值单调递减的队列。<br>时间复杂度：$O(nV)$</p>
<p>具体实现：对于每种物品，由于每种物品在考虑的时候都是独立于其他物品的，只与自己这种物品中的物品个数有关。（不考虑$f[i-1][j-k\times v]$）这种值的影响。<br>那么我们每次先对每种物品枚举模物品体积的余数$j%v=(j-v)%v=…=r$<br>那么每种物品的初始状态就是$f[i][r]$。由于体积为$j$，故$[0,v)$的余数都可能存在，故我们对每种余数的状态都需要维护单调队列单独考虑。<br>对于余数为$r$的状态，其真实体积可以有$r,r+v,…,j-v$，单调队列维护的是价值单调递减的队列，但是是存储体积，以体积作为索引得到当前体积下的价值。<br><strong>代码：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;cstdio&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;cstring&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">f[i][j]只与f[i][j-(1~s)*v]+(1~s)*w 有关 </span></span><br><span class="line"><span class="comment">用这个窗口中的最大值更新f[i][j]  </span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> N = <span class="number">20010</span>;</span><br><span class="line"><span class="keyword">int</span> f[N], g[N], q[N];</span><br><span class="line"><span class="comment">//q是前s个状态的单调递减队列  </span></span><br><span class="line"><span class="keyword">int</span> n, m;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">scanf</span>(<span class="string">"%d%d"</span>, &amp;n, &amp;m);</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">		<span class="keyword">int</span> v, w, s; <span class="built_in">scanf</span>(<span class="string">"%d%d%d"</span>, &amp;v, &amp;w, &amp;s);</span><br><span class="line">		<span class="built_in">memcpy</span>(g, f, <span class="keyword">sizeof</span> f); <span class="comment">//g保存上一层f，是因为更新的时候是用的第i-1层 </span></span><br><span class="line">		<span class="keyword">for</span>(<span class="keyword">int</span> r = <span class="number">0</span>; r &lt; v; r++) &#123;<span class="comment">//枚举余数 </span></span><br><span class="line">			<span class="keyword">int</span> hh = <span class="number">0</span>, tt = <span class="number">-1</span>;</span><br><span class="line">			<span class="keyword">for</span>(<span class="keyword">int</span> k = r; k &lt;= m; k += v) &#123; <span class="comment">//依次更新f[i][j],f[i][j-v],...,f[i][j-s*v] </span></span><br><span class="line">				<span class="keyword">if</span>(hh &lt;= tt &amp;&amp; q[hh] + s * v &lt; k) hh++;<span class="comment">//队头已经不止选s个物品，故弹出队头 </span></span><br><span class="line">				<span class="keyword">while</span>(hh &lt;= tt &amp;&amp; g[q[tt]] - (q[tt] - r) / v * w &lt;= g[k] - (k - r) / v * w) tt--; </span><br><span class="line">		      <span class="comment">/*维护队列单调递减，因为是从余数r开始枚举每种状态，</span></span><br><span class="line"><span class="comment">				这里注意g储存的是f[i-1][j-x*v]的值，而减去(x-r)/v*w是为了保证他们的值一样 </span></span><br><span class="line"><span class="comment">				如f[i-1][r]，以及f[i-1][j-v]相当于f[i-1][r+(s-1)*v] </span></span><br><span class="line"><span class="comment">				由于两者的体积不一样，那么必然体积大的价值高 </span></span><br><span class="line"><span class="comment">				所以我们需要将其限制在同样的条件下，f[i-1][(s-1)*v]多加了(s-1)*w</span></span><br><span class="line"><span class="comment">				那么减去f[i-1][(s-1)*v]减去(s-1)*w相当于体积也被限制成了r，那么两者就处于同等条件下 </span></span><br><span class="line"><span class="comment">				就可以比较了*/</span> </span><br><span class="line">				q[++tt] = k; </span><br><span class="line">				f[k] = g[q[hh]] + (k - q[hh]) / v * w;</span><br><span class="line">				<span class="comment">//f[i][k] = f[i][j-x*v]+x*w  </span></span><br><span class="line">				<span class="comment">//k相当于j，而q[hh]相当于j-x*v,那么就是k-q[hh]=x*v，加了x的物品价值 x*v/v*w=x*w  </span></span><br><span class="line">			&#125;</span><br><span class="line">		&#125; </span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">"%d\n"</span>, f[m]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>对于上述的另一种理解方式：其实就是比较实际大小。<br>两种参数无法直接比较，那么可以这样：<br>$f[j]=max(f[j-v]+w,f[j-2v]+2w,f[j-3v]+3w)$<br>当$j=5v,f[5v]=max(f[4v]+w,f[3v]+2w,f[2v]+3w)$，可以先统一先减去$5w$，<br>得到相对大小的最大值后后再加上$5w$<br>$f[5v]=max(f[4v]-4w,f[3v]-3w,f[2v]-2w)+5w$<br>这样内部就是我们上述代码中的：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span>(hh &lt;= tt &amp;&amp; g[q[tt]] - (q[tt] - r) / v * w &lt;= g[k] - (k - r) / v * w) hh++;</span><br></pre></td></tr></table></figure>
<hr>
<p>$4.$混合背包：对三种背包单独做相应的状态即可。<br>$5.$可拆分背包：按单位价值排序后直接做即可</p>
]]></content>
      <categories>
        <category>基础算法</category>
      </categories>
      <tags>
        <tag>背包问题</tag>
      </tags>
  </entry>
</search>
